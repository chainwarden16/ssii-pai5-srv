/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn how to create Gradle builds at https://guides.gradle.org/creating-new-gradle-builds/
 */

plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // Apply the application plugin to add support for building an application
    id 'application'
}

// Define the main class for the application
mainClassName = 'src.Main'

dependencies {
    // This dependency is found on compile classpath of this component and consumers.
    compile 'com.google.guava:guava:23.0', "apache-log4j:log4j:1.2.15", files('libs/sqlite-jdbc-3.21.0.jar')
    compile group: 'com.googlecode.json-simple', name: 'json-simple', version: '1.1'
    

    // Use JUnit test framework
    testCompile 'junit:junit:4.12', "apache-log4j:log4j:1.2.15"
}

// Para permitir que en las pruebas salga el stdout
test {
    testLogging.showStandardStreams = true
}

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
    mavenCentral()
}
